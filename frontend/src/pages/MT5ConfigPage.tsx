import React from 'react';
import { Container, Typography } from '@mui/material';

const MT5ConfigPage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">MT5 Configuration</Typography>
      <Typography>MT5 account configuration coming soon...</Typography>
    </Container>
  );
};

export default MT5ConfigPage;